/*
  
  0 - rocks
  1 - paper
  2 - scissors

*/

var Game = (function()
{

    let choices = document.querySelectorAll("li");
    let userWins = 0;
    let cpuWins = 0;

    for(let choice_id = 0; choice_id < 3; ++choice_id)
    {
	
	choices[choice_id].onclick = function play()
	{

	    let computerAnswer = () => { return Math.floor(Math.random() * (2 - 0 + 1)) + 0 };

	    
	    evaluateWinner(computerAnswer(), choice_id);
	    
	}

	function evaluateWinner(cpuChoice, userChoice)
	{
	    
	    console.log("cpuChoice: " + cpuChoice);
	    
	    if(cpuChoice == userChoice){
		console.log("tie!");
		return;
	    }

	    switch(userChoice){
	    case 0:
		x
	    }
	    
	}
		
    }
    
    
}) ();
 
